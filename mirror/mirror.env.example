# GPG key ID or fingerprint used to sign Release and InRelease metadata.
APT_GPG_KEY_ID=

# Optional. Supplying a token raises the GitHub API rate limit and is useful on
# shared/public IP addresses. Fine-grained read-only access is sufficient.
GITHUB_TOKEN=
